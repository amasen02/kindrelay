# AI coding disclosure

AI-assisted coding tools and an AI orchestrator were used during development and review. Human final review remains pending. They were not used as a runtime feature of KindRelay.

KindRelay does not call an LLM or external AI service. Its draft suggestions use the documented deterministic `ACTION:`, `TODO:`, and `- [ ]` grammar. Suggestions remain drafts until a human reviews them.
